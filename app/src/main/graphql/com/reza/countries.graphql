query Continent($continent_code: ID!) {
    continent(code: $continent_code) {
        countries {
            name
            emoji
            native
            currency
            phone
            languages {
                name
                rtl
            }
            states {
                name
            }
        }
    }
}